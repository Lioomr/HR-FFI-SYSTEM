# Documentation Synchronization Tasks

- [ ] Read `AGENTS.md`, `SECURITY_POLICY.md`, the mobile master plan, and the route matrix.
- [ ] Verify changed endpoint paths against Django URLs/ViewSets and serializers.
- [ ] Update the API contract and mark legacy/compatibility routes explicitly.
- [ ] Update only affected `.agents/context`, `.agents/rules`, and `.agents/skills` files.
- [ ] Search for stale versions, endpoint examples, and token-storage claims.
- [ ] Run backend tests and frontend type-check/build checks when code behavior is touched.
- [ ] Record unresolved mismatches in `plans/API Route Status Matrix.md`.
- [ ] Preserve unrelated working-tree changes.
