# Local render scratch — regenerate by running `npx hyperframes` inside the
# composition directory (it re-fetches vendor/gsap.min.js on demand).
composition*/node_modules/
composition*/snapshots/
composition*/vendor/
